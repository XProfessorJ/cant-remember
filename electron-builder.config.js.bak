/** @type {import('electron-builder').Configuration} */
module.exports = {
  appId: 'com.cantremember.app',
  productName: 'Can\'t Remember',
  directories: {
    output: 'release',
    buildResources: 'build',
  },
  files: [
    // 明确指定要包含的文件和目录
    // 注意：electron-builder 会将 dist/**/* 的内容展平到 app.asar 根目录
    // 所以 dist/index.html -> app.asar/index.html
    // dist/assets/* -> app.asar/assets/*
    'dist/**/*',
    'electron/main.cjs',
    'electron/preload.cjs',
    'package.json',
    // 排除不需要的文件
    '!.editorconfig',
    '!.eslintrc.cjs',
    '!.npmrc',
    '!.vscode',
    '!.git',
    '!.gitignore',
    '!*.md',
    '!tsconfig*.json',
    '!vite.config.ts',
    '!tailwind.config.js',
    '!postcss.config.js',
    '!*.log',
    '!scripts',
    '!src',
    '!electron/*.ts',
    '!electron/*.js',
    // 明确排除根目录的 index.html（源文件）
    '!index.html',
    '!**/node_modules/*/{CHANGELOG.md,README.md,README,readme.md,readme}',
    '!**/node_modules/*/{test,__tests__,tests,powered-test,example,examples}',
    '!**/node_modules/*.d.ts',
    '!**/node_modules/.bin',
    '!**/*.{iml,o,hprof,orig,pyc,pyo,rbc,swp,csproj,sln,xproj}',
    '!**/._*',
    '!**/{.DS_Store,.git,.hg,.svn,CVS,RCS,SCCS,.gitignore,.gitattributes}',
    '!**/{__pycache__,thumbs.db,.flowconfig,.idea,.vs,.nyc_output}',
    '!**/{appveyor.yml,.travis.yml,circle.yml}',
    '!**/{npm-debug.log,yarn.lock,.yarn-integrity,.yarn-metadata.json}',
  ],
  extraMetadata: {
    main: 'electron/main.cjs',
  },
  asar: true, // 打包成 asar 格式
  mac: {
    category: 'public.app-category.education',
    target: [
      {
        target: 'dmg',
        arch: ['arm64'], // 只构建当前架构，避免下载失败
      },
      {
        target: 'zip',
        arch: ['arm64'],
      },
    ],
    // icon: 'build/icon.icns', // 暂时注释，如果没有图标文件
  },
  win: {
    target: [
      {
        target: 'nsis',
        arch: ['x64'],
      },
    ],
    // icon: 'build/icon.ico', // 暂时注释，如果没有图标文件
  },
  linux: {
    target: [
      {
        target: 'AppImage',
        arch: ['x64'],
      },
    ],
    // icon: 'build/icon.png', // 暂时注释，如果没有图标文件
    category: 'Education',
  },
  nsis: {
    oneClick: false,
    allowToChangeInstallationDirectory: true,
    createDesktopShortcut: true,
    createStartMenuShortcut: true,
  },
  publish: null, // 如果需要自动更新，可以配置发布服务器
};

